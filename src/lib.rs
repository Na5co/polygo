//! polygo — local-first, git-native localization.
//!
//! The library exposes format parsers/serializers (byte-stable round-trips are a
//! hard requirement) and, later, the lockfile, providers and validators.

pub mod formats;
